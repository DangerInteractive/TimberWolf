#ifndef H_TW_KEYACTION
#define H_TW_KEYACTION

/**
 * @file
 *
 * Header file for the tw::KeyAction enum.
 */

namespace tw {
enum KeyAction {

    PRESS = 1,
    RELEASE = 0,
    REPEAT = 2

};
}

#endif
